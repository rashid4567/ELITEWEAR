<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ELITE WEAR | Redefining Modern Fashion</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css" />
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />
    <link rel="stylesheet" href="/userStyle/header.css">
    <link rel="stylesheet" href="/userStyle/footer.css">
    <link rel="stylesheet" href="/userStyle/trending-section-styles.css">
    <link rel="stylesheet" href="/userStyle/customer-reviews.css">
    <link rel="stylesheet" href="/userStyle/home.css">
</head>

<body>


    <!-- Scroll Indicator -->
    <div class="scroll-indicator"></div>

    <!-- Cursor Glow Effect -->
    <div class="cursor-glow"></div>

    <%- include("../partials/user/header") %>

    <!-- Hero Carousel Section -->
    <section class="hero-section">
        <div class="swiper hero-swiper">
            <div class="swiper-wrapper">
                <% if (Banner && Banner.length > 0) { %>
                    <% Banner.forEach((banner) => { %>
                        <div class="swiper-slide">
                            <div class="hero-slide" style="background-image: url('<%= banner.image %>');">
                                <span class="futuristic-badge"><%= banner.title %></span>
                                <div class="hero-content" data-aos="fade-up" data-aos-delay="200">
                                    <h1 class="hero-title"><%= banner.heading %></h1>
                                    <p class="hero-subtitle"><%= banner.description %></p>
                                    <a href="<%= banner.buttonLink %>" class="btn-shop-now"><%= banner.buttonText %></a>
                                </div>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <!-- Default banners if none are available -->
                    <div class="swiper-slide">
                        <div class="hero-slide" style="background-image: url('/Uploads/LP-Mos_main_banner-Summer-Banner-D.jpg');">
                            <span class="futuristic-badge">New Collection</span>
                            <div class="hero-content" data-aos="fade-up" data-aos-delay="200">
                                <h1 class="hero-title">Redefine Your Style</h1>
                                <p class="hero-subtitle">Discover our premium collection crafted for the modern individual who appreciates elegance and comfort</p>
                                <a href="/allproduct" class="btn-shop-now">Explore Collection</a>
                            </div>
                        </div>
                    </div>
                    <div class="swiper-slide">
                        <div class="hero-slide" style="background-image: url('/Uploads/LP_Hero Banner_linen_D-2.jpg');">
                            <span class="futuristic-badge">Premium Linen</span>
                            <div class="hero-content" data-aos="fade-up" data-aos-delay="200">
                                <h1 class="hero-title">Breathable Luxury</h1>
                                <p class="hero-subtitle">Experience the perfect blend of comfort and sophistication with our premium linen collection</p>
                                <a href="/allproduct" class="btn-shop-now">Shop Now</a>
                            </div>
                        </div>
                    </div>
                    <div class="swiper-slide">
                        <div class="hero-slide" style="background-image: url('/Uploads/timesnap.jpeg');">
                            <span class="futuristic-badge">Formal Collection</span>
                            <div class="hero-content" data-aos="fade-up" data-aos-delay="200">
                                <h1 class="hero-title">Elevate Your Presence</h1>
                                <p class="hero-subtitle">Make a lasting impression with our meticulously crafted formal wear designed for the modern professional</p>
                                <a href="/allproduct" class="btn-shop-now">View Collection</a>
                            </div>
                        </div>
                    </div>
                <% } %>
            </div>
            <div class="swiper-pagination"></div>
            <div class="swiper-button-next"></div>
            <div class="swiper-button-prev"></div>
        </div>
    </section>

    <% if (error) { %>
    <section class="error-section">
        <div class="container">
            <p class="error-message text-danger text-center"><%= error %></p>
        </div>
    </section>
    <% } %>

    <!-- Categories Section -->
    <section class="categories-section">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">Curated Collections</h2>
            </div>
            <div class="row g-4">
                <% 
                // Get four product images from the data array
                const categoryImages = [];
                const categoryTitles = ['Formal Elegance', 'Casual Comfort', 'Premium Linen', 'Designer Prints'];
                const categoryLinks = ['formal', 'casual', 'linen', 'printed'];
                
                if (data && Array.isArray(data) && data.length > 0) {
                    // Get up to 4 products with images
                    const productsWithImages = data.filter(p => p.images && p.images.length > 0).slice(0, 4);
                    
                    // Add product images to the categoryImages array
                    productsWithImages.forEach(product => {
                        if (product.images && product.images.length > 0) {
                            categoryImages.push(product.images[0].url);
                        }
                    });
                }
                
                // Fill with placeholders if we don't have enough product images
                while (categoryImages.length < 4) {
                    categoryImages.push('/placeholder.svg?height=300&width=300&query=shirt');
                }
                
                // Render the category cards
                for (let i = 0; i < 4; i++) {
                %>
                    <div class="col-6 col-md-3" data-aos="fade-up" data-aos-delay="<%= i * 100 %>">
                        <a href="/allproduct?category=<%= categoryLinks[i] %>" class="category-card glow-effect">
                            <div class="category-image">
                                <img src="<%= categoryImages[i] %>" alt="<%= categoryTitles[i] %>" class="product-image">
                            </div>
                            <h3 class="category-title"><%= categoryTitles[i] %></h3>
                        </a>
                    </div>
                <% } %>
            </div>
        </div>
    </section>

    <!-- New Arrivals Section -->
    <section class="new-arrivals-section">
        <div class="container">
            <div class="section-header" data-aos="fade-up">
                <h2 class="section-title">New Arrivals</h2>
                <a href="/allproduct" class="view-all">View All <i class="fas fa-arrow-right"></i></a>
            </div>
            
            <div class="swiper products-swiper" data-aos="fade-up" data-aos-delay="200">
                <div class="swiper-wrapper">
                    <% if (data && Array.isArray(data) && data.length > 0) { %>
                        <% data.forEach((product) => { %>
                            <div class="swiper-slide">
                                <div class="product-card">
                                    <div class="wishlist-icon">
                                        <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="wishlist-heart" data-product-id="<%= product._id %>">
                                            <path class="heart" d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                                        </svg>
                                    </div>
                                    <a href="/productdetails/<%= product._id %>" class="product-image-link">
                                        <img 
                                            src="<%= product.images && product.images.length > 0 ? product.images[0].url : '/placeholder.svg?height=300&width=300&query=shirt' %>" 
                                            alt="<%= product.name || 'Product Image' %>" 
                                            class="product-image"
                                            onerror="this.onerror=null; this.src='/placeholder.svg?height=300&width=300&query=shirt';"
                                        >
                                        <div class="product-overlay">
                                            <span class="quick-view">Quick View</span>
                                        </div>
                                    </a>
                                    <div class="product-info">
                                        <h3 class="product-name"><%= product.name || 'Unnamed Product' %></h3>
                                        <div class="product-price-rating">
                                            <p class="product-price">₹<%= (product.variants && product.variants.length > 0 ? product.variants[0].salePrice : 0).toFixed(2) %></p>
                                            <div class="star-rating">
                                                <% const rating = product.ratings && product.ratings.average ? product.ratings.average : 0;
                                                    for(let i = 1; i <= 5; i++) {
                                                        if(i <= rating) { %>
                                                            <i class="fas fa-star"></i>
                                                        <% } else if(i - 0.5 <= rating) { %>
                                                            <i class="fas fa-star-half-alt"></i>
                                                        <% } else { %>
                                                            <i class="far fa-star"></i>
                                                        <% } 
                                                    } 
                                                %>
                                            </div>
                                        </div>
                                        <a href="/productdetails/<%= product._id %>" class="btn-view-product">View Product</a>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    <% } else { %>
                        <div class="swiper-slide">
                            <p class="no-products">No new arrivals available at this time.</p>
                        </div>
                    <% } %>
                </div>
                <div class="swiper-pagination"></div>
                <div class="swiper-button-next"></div>
                <div class="swiper-button-prev"></div>
            </div>
        </div>
    </section>

    <!-- Featured Banner -->
    <section class="featured-banner">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-6" data-aos="fade-right">
                    <div class="banner-card" style="background-image: url('/Uploads/LP_Hero Banner_linen_D-2.jpg');">
                        <div class="banner-content">
                            <h3>Summer Essentials</h3>
                            <p>Breathable fabrics designed for comfort in warm weather</p>
                            <a href="/allproduct?category=summer" class="btn-banner">Explore</a>
                        </div>
                    </div>
                </div>
                <div class="col-md-6" data-aos="fade-left">
                    <div class="banner-card" style="background-image: url('/Uploads/Screenshot 2025-04-07 201605.png');">
                        <div class="banner-content">
                            <h3>Professional Collection</h3>
                            <p>Elevate your workwear with our premium formal attire</p>
                            <a href="/allproduct?category=formal" class="btn-banner">Discover</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Trending Products Section -->
<section class="trending-products-section">
  <div class="container">
    <div class="section-header" data-aos="fade-up">
      <h2 class="section-title">Trending Now</h2>
      <a href="/allproduct?sort=popular" class="view-all">View All <i class="fas fa-arrow-right"></i></a>
    </div>
    
    <div class="trending-grid">
      <% if (data && Array.isArray(data) && data.length > 0) { %>
        <% data.slice(0, 4).forEach((product, index) => { %>
          <div class="trending-item" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
            <div class="product-card">
              <div class="product-card-inner">
                <div class="wishlist-icon">
                  <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="wishlist-heart" data-product-id="<%= product._id %>">
                    <path class="heart" d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                  </svg>
                </div>
                <a href="/productdetails/<%= product._id %>" class="product-image-link1">
                  <div class="product-image-wrapper">
                    <% 
                    let imageUrl = '/placeholder.svg?height=300&width=300&query=fashion+product';
                    if (product.images && product.images.length > 0 && product.images[0].url) {
                      imageUrl = product.images[0].url;
                    }
                    %>
                    <img 
                      src="<%= imageUrl %>" 
                      alt="<%= product.name || 'Product Image' %>" 
                      class="product-image"
                      style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; display: block !important; opacity: 1 !important; visibility: visible !important; z-index: 1;"
                      onerror="this.onerror=null; this.src='/placeholder.svg?height=300&width=300&query=fashion+product';"
                    >
                  </div>
                </a>
                <div class="product-info">
                  <div class="product-badge">
                    <span>Trending</span>
                  </div>
                  <h3 class="product-name"><%= product.name || 'Unnamed Product' %></h3>
                  <div class="product-price-rating">
                    <p class="product-price">₹<%= (product.variants && product.variants.length > 0 ? product.variants[0].salePrice : 0).toFixed(2) %></p>
                    <div class="star-rating">
                      <% const rating = product.ratings && product.ratings.average ? product.ratings.average : 0;
                        for(let i = 1; i <= 5; i++) {
                          if(i <= rating) { %>
                            <i class="fas fa-star"></i>
                          <% } else if(i - 0.5 <= rating) { %>
                            <i class="fas fa-star-half-alt"></i>
                          <% } else { %>
                            <i class="far fa-star"></i>
                          <% } 
                        } 
                      %>
                    </div>
                  </div>
                  <a href="/productdetails/<%= product._id %>" class="btn-view-product">
                    View Product <i class="fas fa-arrow-right"></i>
                  </a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <!-- Fallback products if no data is available -->
        <% 
        const fallbackProducts = [
          { 
            name: "Signature Twill Paisley Effect Shirt", 
            price: 3612.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=pink%20dress%20shirt" 
          },
          { 
            name: "Solid Merino Wool Shirt", 
            price: 18275.00, 
            rating: 5, 
            image: "/placeholder.svg?height=300&width=300&query=brown%20dress%20shirt" 
          },
          { 
            name: "Paisley Pulse Twill Shirt", 
            price: 3807.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=paisley%20pattern%20shirt" 
          },
          { 
            name: "Solid Signature Twill Shirt", 
            price: 5727.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=cream%20dress%20shirt" 
          }
        ];
        
        fallbackProducts.forEach((product, index) => { 
        %>
          <div class="trending-item" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
            <div class="product-card">
              <div class="product-card-inner">
                <div class="wishlist-icon">
                  <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="wishlist-heart">
                    <path class="heart" d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                  </svg>
                </div>
                <a href="/allproduct" class="product-image-link1">
                  <div class="product-image-wrapper">
                    <img 
                      src="<%= product.image %>" 
                      alt="<%= product.name %>" 
                      class="product-image"
                      style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; object-fit: cover; display: block !important; opacity: 1 !important; visibility: visible !important; z-index: 1;"
                      onerror="this.onerror=null; this.src='/placeholder.svg?height=300&width=300&query=fashion+product';"
                    >
                  </div>
                </a>
                <div class="product-info">
                  <div class="product-badge">
                    <span>Trending</span>
                  </div>
                  <h3 class="product-name"><%= product.name %></h3>
                  <div class="product-price-rating">
                    <p class="product-price">₹<%= product.price.toFixed(2) %></p>
                    <div class="star-rating">
                      <% for(let i = 1; i <= 5; i++) {
                        if(i <= Math.floor(product.rating)) { %>
                          <i class="fas fa-star"></i>
                        <% } else if(i - 0.5 <= product.rating) { %>
                          <i class="fas fa-star-half-alt"></i>
                        <% } else { %>
                          <i class="far fa-star"></i>
                        <% } 
                      } %>
                    </div>
                  </div>
                  <a href="/allproduct" class="btn-view-product">
                    View Product <i class="fas fa-arrow-right"></i>
                  </a>
                </div>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>
</section>

    <!-- Add this script to fix image rendering issues -->
    <script>
    document.addEventListener("DOMContentLoaded", () => {
      // Fix for trending product images
      const trendingImages = document.querySelectorAll('.trending-item .product-image');
      
      trendingImages.forEach((img) => {
        // Force image display properties
        img.style.display = "block";
        img.style.opacity = "1";
        img.style.visibility = "visible";
        
        // Add error handling
        img.onerror = function() {
          this.src = '/placeholder.svg?height=300&width=300&query=fashion+product';
        };
      });
    });
    </script>

    <!-- Collection Showcase with Video -->
    <section class="collection-showcase">
        <div class="container">
            <div class="collection-card" data-aos="fade-up">
                <div class="collection-video-container">
                    <video class="collection-video" autoplay loop muted playsinline>
                        <source src="/videos/formal-collection.mp4" type="video/mp4">
                        <!-- Fallback image if video doesn't load -->
                        <img src="/Uploads/Screenshot 2025-04-07 201605.png" alt="Formal Collection" class="collection-video">
                    </video>
                    <div class="video-overlay"></div>
                </div>
                <div class="collection-content">
                    <h2>REFINED FORMAL COLLECTION</h2>
                    <p>Elevate your professional wardrobe with our premium formal collection. Each piece is meticulously crafted with the finest fabrics and impeccable tailoring to ensure you make a lasting impression in any professional setting.</p>
                    <div class="collection-features">
                        <div class="feature">
                            <i class="fas fa-tshirt"></i>
                            <span>Premium Fabrics</span>
                        </div>
                        <div class="feature">
                            <i class="fas fa-ruler"></i>
                            <span>Tailored Fit</span>
                        </div>
                        <div class="feature">
                            <i class="fas fa-medal"></i>
                            <span>Superior Quality</span>
                        </div>
                    </div>
                    <a href="/allproduct?category=formal" class="btn-collection">Explore Collection</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Customer Reviews Section -->
    <section class="customer-reviews-section">
        <div class="container">
            <h2 class="section-title text-center" data-aos="fade-up">What Our Customers Say</h2>
            
            <div class="reviews-container" data-aos="fade-up" data-aos-delay="200">
                <div class="swiper reviews-swiper">
                    <div class="swiper-wrapper">
                        <% if (reviews && reviews.length > 0) { %>
                            <% reviews.forEach((review, index) => { %>
                                <div class="swiper-slide">
                                    <div class="review-card" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                                        <div class="shimmer"></div>
                                        <div class="review-header">
                                            <div class="review-rating">
                                                <% for(let i = 1; i <= 5; i++) { %>
                                                    <% if(i <= review.rating) { %>
                                                        <i class="fas fa-star"></i>
                                                    <% } else { %>
                                                        <i class="far fa-star"></i>
                                                    <% } %>
                                                <% } %>
                                            </div>
                                            <div class="review-product">
                                                <span>on <%= review.product ? review.product.name : 'Product' %></span>
                                            </div>
                                        </div>
                                        
                                        <h4 class="review-title"><%= review.title %></h4>
                                        
                                        <div class="review-content">
                                            <p class="review-text"><%= review.description %></p>
                                        </div>
                                        
                                        <div class="review-footer">
                                            <div class="reviewer">
                                                <div class="reviewer-avatar">
                                                    <% if (review.user && review.user.profileImage) { %>
                                                        <img src="<%= review.user.profileImage %>" alt="<%= review.user.fullname %>">
                                                    <% } else { %>
                                                        <div class="avatar-placeholder">
                                                            <%= review.user && review.user.fullname ? review.user.fullname.charAt(0).toUpperCase() : 'U' %>
                                                        </div>
                                                    <% } %>
                                                </div>
                                                <div class="reviewer-info">
                                                    <h5><%= review.user ? review.user.fullname : 'Anonymous' %></h5>
                                                    <p>Verified Buyer</p>
                                                </div>
                                            </div>
                                            <div class="review-date">
                                                <%= new Date(review.createdAt).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                                            </div>
                                        </div>
                                        
                                        <% if (review.helpfulVotes && review.helpfulVotes.length > 0) { %>
                                            <div class="helpful-badge">
                                                <i class="fas fa-thumbs-up"></i> <%= review.helpfulVotes.length %> found this helpful
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            <% }) %>
                        <% } else { %>
                            <!-- Fallback testimonials if no reviews are available -->
                            <div class="swiper-slide">
                                <div class="review-card">
                                    <div class="shimmer"></div>
                                    <div class="review-header">
                                        <div class="review-rating">
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                        </div>
                                    </div>
                                    <h4 class="review-title">Exceptional Quality</h4>
                                    <div class="review-content">
                                        <p class="review-text">"The quality of shirts from Elite Wear is exceptional. The fabric feels premium and the fit is perfect. I've received numerous compliments whenever I wear them. Definitely my go-to brand for formal wear."</p>
                                    </div>
                                    <div class="review-footer">
                                        <div class="reviewer">
                                            <div class="reviewer-avatar">
                                                <div class="avatar-placeholder">R</div>
                                            </div>
                                            <div class="reviewer-info">
                                                <h5>Rajesh Kumar</h5>
                                                <p>Verified Buyer</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="review-card">
                                    <div class="shimmer"></div>
                                    <div class="review-header">
                                        <div class="review-rating">
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star-half-alt"></i>
                                        </div>
                                    </div>
                                    <h4 class="review-title">Always Up-to-Date</h4>
                                    <div class="review-content">
                                        <p class="review-text">"I've been shopping with Elite Wear for over a year now. Their collection is always up-to-date with the latest trends, and the quality never disappoints. The customer service is also exceptional."</p>
                                    </div>
                                    <div class="review-footer">
                                        <div class="reviewer">
                                            <div class="reviewer-avatar">
                                                <div class="avatar-placeholder">V</div>
                                            </div>
                                            <div class="reviewer-info">
                                                <h5>Vikram Singh</h5>
                                                <p>Loyal Customer</p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="swiper-slide">
                                <div class="review-card">
                                    <div class="shimmer"></div>
                                    <div class="review-header">
                                        <div class="review-rating">
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                            <i class="fas fa-star"></i>
                                        </div>
                                    </div>
                                    <h4 class="review-title">Remarkable Attention to Detail</h4>
                                    <div class="review-content">
                                        <p class="review-text">"The attention to detail in Elite Wear shirts is remarkable. From the stitching to the buttons, everything speaks quality. The linen collection is perfect for summer, breathable yet elegant. Highly recommend!"</p>
                                    </div>
                                    <div class="review-footer">
                                        <div class="reviewer">
                                            <div class="reviewer-avatar">
                                                <div class="avatar-placeholder">A</div>
                                            </div>
                                         <h5><%= fullName %></h5>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                    </div>
                    
                    <div class="swiper-pagination"></div>
                    
                    <div class="reviews-navigation">
                        <div class="reviews-button-prev">
                            <i class="fas fa-chevron-left"></i>
                        </div>
                        <div class="reviews-button-next">
                            <i class="fas fa-chevron-right"></i>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="reviews-cta" data-aos="fade-up">
                <a href="/allproduct" class="btn-view-all-reviews">
                    <span>Explore</span>
                    <i class="fas fa-arrow-right"></i>
                </a>
            </div>
        </div>
    </section>

    <!-- Newsletter Section -->
    <section class="newsletter-section">
        <div class="container">
            <div class="newsletter-container">
                <div class="newsletter-content" data-aos="fade-right">
                    <h2>Join Our Style Community</h2>
                    <p>Subscribe to receive exclusive offers, early access to new collections, and personalized style recommendations.</p>
                    <form class="newsletter-form">
                        <div class="form-group">
                            <input type="email" placeholder="Your email address" required>
                            <button type="submit">Subscribe</button>
                        </div>
                        <div class="form-check">
                            <input type="checkbox" id="newsletter-consent" required>
                            <label for="newsletter-consent">I agree to receive marketing emails from Elite Wear</label>
                        </div>
                    </form>
                </div>
                <div class="newsletter-image" data-aos="fade-left">
                    <img src="/placeholder.svg?height=300&width=300&query=fashion newsletter" alt="Newsletter">
                </div>
            </div>
        </div>
    </section>

    <%- include("../partials/user/footer") %>

    <!-- Toast Container -->
    <div class="toast-container">
        <div id="wishlistToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-content">
                <i class="toast-icon fas fa-check-circle"></i>
                <div class="toast-message" id="toastBody"></div>
            </div>
            <button class="toast-close" data-bs-dismiss="toast" aria-label="Close">
                <i class="fas fa-times"></i>
            </button>
        </div>
    </div>

    <!-- Quick View Modal -->
    <div class="modal fade" id="quickViewModal" tabindex="-1" aria-labelledby="quickViewModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="quickViewModalLabel">Quick View</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-md-6">
                            <img id="quickViewImage" src="/placeholder.svg" alt="Product Image" class="img-fluid rounded">
                        </div>
                        <div class="col-md-6">
                            <h3 id="quickViewName" class="mb-3"></h3>
                            <p id="quickViewPrice" class="product-price mb-3"></p>
                            <div id="quickViewRating" class="star-rating mb-3"></div>
                            <p id="quickViewDescription" class="mb-4"></p>
                            <div class="d-grid">
                                <a id="quickViewLink" href="#" class="btn-primary">View Details</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Dark Mode Toggle -->
    <button class="dark-mode-toggle" id="darkModeToggle">
        <i class="fas fa-moon"></i>
    </button>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialize AOS
            AOS.init({
                duration: 800,
                easing: 'ease-in-out',
                once: true
            });

            // Preloader
            const preloader = document.querySelector('.preloader');
            window.addEventListener('load', () => {
                setTimeout(() => {
                    preloader.classList.add('fade-out');
                }, 500);
            });

            // Scroll Indicator
            const scrollIndicator = document.querySelector('.scroll-indicator');
            window.addEventListener('scroll', () => {
                const winScroll = document.body.scrollTop || document.documentElement.scrollTop;
                const height = document.documentElement.scrollHeight - document.documentElement.clientHeight;
                const scrolled = (winScroll / height) * 100;
                scrollIndicator.style.width = scrolled + '%';
            });

            // Cursor Glow Effect
            const cursorGlow = document.querySelector('.cursor-glow');
            document.addEventListener('mousemove', (e) => {
                cursorGlow.style.left = e.clientX + 'px';
                cursorGlow.style.top = e.clientY + 'px';
            });

            document.addEventListener('mousedown', () => {
                cursorGlow.style.width = '30px';
                cursorGlow.style.height = '30px';
                cursorGlow.style.opacity = '0.8';
            });

            document.addEventListener('mouseup', () => {
                cursorGlow.style.width = '20px';
                cursorGlow.style.height = '20px';
                cursorGlow.style.opacity = '0.5';
            });

            // Initialize Swiper carousels
            const heroSwiper = new Swiper('.hero-swiper', {
                slidesPerView: 1,
                spaceBetween: 0,
                loop: true,
                autoplay: {
                    delay: 5000,
                    disableOnInteraction: false,
                },
                pagination: {
                    el: '.hero-swiper .swiper-pagination',
                    clickable: true,
                },
                navigation: {
                    nextEl: '.hero-swiper .swiper-button-next',
                    prevEl: '.hero-swiper .swiper-button-prev',
                },
                effect: 'fade',
                fadeEffect: {
                    crossFade: true
                },
            });

            const productsSwiper = new Swiper('.products-swiper', {
                slidesPerView: 1,
                spaceBetween: 20,
                loop: true,
                autoplay: {
                    delay: 4000,
                    disableOnInteraction: false,
                },
                pagination: {
                    el: '.products-swiper .swiper-pagination',
                    clickable: true,
                },
                navigation: {
                    nextEl: '.products-swiper .swiper-button-next',
                    prevEl: '.products-swiper .swiper-button-prev',
                },
                breakpoints: {
                    576: {
                        slidesPerView: 2,
                    },
                    768: {
                        slidesPerView: 3,
                    },
                    992: {
                        slidesPerView: 4,
                    },
                },
            });

            // Initialize Reviews Swiper
            const reviewsSwiper = new Swiper('.reviews-swiper', {
                slidesPerView: 1,
                spaceBetween: 20,
                loop: true,
                autoplay: {
                    delay: 5000,
                    disableOnInteraction: false,
                },
                pagination: {
                    el: '.reviews-swiper .swiper-pagination',
                    clickable: true,
                },
                navigation: {
                    nextEl: '.reviews-button-next',
                    prevEl: '.reviews-button-prev',
                },
                breakpoints: {
                    640: {
                        slidesPerView: 1,
                        spaceBetween: 20,
                    },
                    768: {
                        slidesPerView: 2,
                        spaceBetween: 30,
                    },
                    1024: {
                        slidesPerView: 3,
                        spaceBetween: 30,
                    },
                },
            });

            // Toast functionality
            const wishlistToast = new bootstrap.Toast(document.getElementById('wishlistToast'));

            async function loadWishlistState() {
                try {
                    const response = await fetch('/wishlist/ids');
                    const data = await response.json();
                
                    document.querySelectorAll('.wishlist-heart').forEach(heart => {
                        const productId = heart.getAttribute('data-product-id');
                        if (data.products && data.products.includes(productId)) {
                            heart.querySelector('.heart').classList.add('active');
                        }
                    });
                } catch (error) {
                    console.error('Error loading wishlist state:', error);
                }
            }
            loadWishlistState();

            
            document.querySelectorAll('.wishlist-heart').forEach(heart => {
                heart.addEventListener('click', async function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    const productId = this.getAttribute('data-product-id');
                    const heartPath = this.querySelector('.heart');
                    const isActive = heartPath.classList.contains('active');
                    const url = isActive ? '/wishlist/remove' : '/addTowhislist';

                    // Add click animation
                    this.closest('.wishlist-icon').classList.add('clicked');
                    setTimeout(() => {
                        this.closest('.wishlist-icon').classList.remove('clicked');
                    }, 300);

                    try {
                        const response = await fetch(url, {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                            },
                            body: JSON.stringify({ productId: productId }),
                        });
                        
                        const data = await response.json();
                    
                        const toastBody = document.getElementById('toastBody');
                        toastBody.textContent = data.message;
                        
                        const toastElement = document.getElementById('wishlistToast');
                        const toastIcon = toastElement.querySelector('.toast-icon');
                        
                        if (data.success) {
                            toastElement.classList.remove('toast-error');
                            toastElement.classList.add('toast-success');
                            toastIcon.className = 'toast-icon fas fa-check-circle';
                            heartPath.classList.toggle('active');

                            // Show floating heart animation if adding to wishlist
                            if (!isActive) {
                                const icon = this.closest('.wishlist-icon');
                                const floatingHeart = document.createElement('div');
                                floatingHeart.className = 'floating-heart';
                                floatingHeart.innerHTML = '<i class="fas fa-heart"></i>';
                                icon.appendChild(floatingHeart);

                                setTimeout(() => {
                                    floatingHeart.remove();
                                }, 1000);
                            }
                        } else {
                            toastElement.classList.remove('toast-success');
                            toastElement.classList.add('toast-error');
                            toastIcon.className = 'toast-icon fas fa-exclamation-circle';
                        }
                        
                        wishlistToast.show();
                    } catch (error) {
                        console.error('Error:', error);
                        const toastBody = document.getElementById('toastBody');
                        toastBody.textContent = 'Operation failed';
                        
                        const toastElement = document.getElementById('wishlistToast');
                        toastElement.classList.remove('toast-success');
                        toastElement.classList.add('toast-error');
                        toastElement.querySelector('.toast-icon').className = 'toast-icon fas fa-exclamation-circle';
                        
                        wishlistToast.show();
                    }
                });
            });

            // Quick View functionality
            document.querySelectorAll('.product-overlay').forEach(overlay => {
                overlay.addEventListener('click', function(e) {
                    if (e.target.classList.contains('quick-view')) {
                        e.preventDefault();
                        e.stopPropagation();
                        
                        const productCard = this.closest('.product-card') || this.closest('.product-card-inner').parentElement;
                        const productLink = productCard.querySelector('.product-image-link').getAttribute('href');
                        const productImage = productCard.querySelector('.product-image').getAttribute('src');
                        const productName = productCard.querySelector('.product-name').textContent;
                        const productPrice = productCard.querySelector('.product-price').textContent;
                        const productRating = productCard.querySelector('.star-rating').innerHTML;
                        
                        document.getElementById('quickViewImage').setAttribute('src', productImage);
                        document.getElementById('quickViewName').textContent = productName;
                        document.getElementById('quickViewPrice').textContent = productPrice;
                        document.getElementById('quickViewRating').innerHTML = productRating;
                        document.getElementById('quickViewDescription').textContent = 'Premium quality product crafted with the finest materials for the modern individual. This item features exceptional craftsmanship and attention to detail.';
                        document.getElementById('quickViewLink').setAttribute('href', productLink);
                        
                        const modal = new bootstrap.Modal(document.getElementById('quickViewModal'));
                        modal.show();
                    }
                });
            });

            // Navbar scroll effect
            window.addEventListener('scroll', function () {
                const navbar = document.querySelector('.navbar');
                if (navbar) {
                    if (window.scrollY > 50) {
                        navbar.classList.add('navbar-scrolled');
                    } else {
                        navbar.classList.remove('navbar-scrolled');
                    }
                }
            });

            // Trending Items Animation
            const trendingItems = document.querySelectorAll('.trending-item');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('show');
                    }
                });
            }, { threshold: 0.1 });
            
            trendingItems.forEach(item => observer.observe(item));

            // Parallax effect for trending product cards
            document.addEventListener('mousemove', (e) => {
                const trendingSection = document.querySelector('.trending-products-section');
                if (!trendingSection) return;

                const sectionBounds = trendingSection.getBoundingClientRect();
                if (
                    e.clientY >= sectionBounds.top &&
                    e.clientY <= sectionBounds.bottom &&
                    e.clientX >= sectionBounds.left &&
                    e.clientX <= sectionBounds.right
                ) {
                    const cards = document.querySelectorAll('.trending-item .product-card');
                    cards.forEach((card) => {
                        const cardBounds = card.getBoundingClientRect();
                        const cardCenterX = cardBounds.left + cardBounds.width / 2;
                        const cardCenterY = cardBounds.top + cardBounds.height / 2;

                        const deltaX = (e.clientX - cardCenterX) / 30;
                        const deltaY = (e.clientY - cardCenterY) / 30;

                        // Apply subtle rotation based on mouse position
                        card.style.transform = `perspective(1000px) rotateY(${-deltaX * 0.5}deg) rotateX(${deltaY * 0.5}deg)`;
                    });
                } else {
                    // Reset all cards when mouse leaves section
                    const cards = document.querySelectorAll('.trending-item .product-card');
                    cards.forEach((card) => {
                        card.style.transform = "perspective(1000px) rotateY(0deg) rotateX(0deg)";
                    });
                }
            });
            
            // Newsletter form submission
            document.querySelector('.newsletter-form')?.addEventListener('submit', function(e) {
                e.preventDefault();
                
                const email = this.querySelector('input[type="email"]').value;
                
                // Here you would typically send this to your server
                // For now, we'll just show a success message
                const toastBody = document.getElementById('toastBody');
                toastBody.textContent = `Thank you for subscribing with ${email}!`;
                
                const toastElement = document.getElementById('wishlistToast');
                toastElement.classList.remove('toast-error');
                toastElement.classList.add('toast-success');
                toastElement.querySelector('.toast-icon').className = 'toast-icon fas fa-check-circle';
                
                wishlistToast.show();
                
                // Reset the form
                this.reset();
            });
            
            // Dark mode toggle
            const darkModeToggle = document.getElementById('darkModeToggle');
            const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');
            
            // Check for saved theme preference or use the system preference
            const savedTheme = localStorage.getItem('theme');
            
            if (savedTheme === 'dark' || (!savedTheme && prefersDarkScheme.matches)) {
                document.body.classList.add('dark-mode');
                darkModeToggle.innerHTML = '<i class="fas fa-sun"></i>';
            }
            
            darkModeToggle.addEventListener('click', () => {
                if (document.body.classList.contains('dark-mode')) {
                    document.body.classList.remove('dark-mode');
                    localStorage.setItem('theme', 'light');
                    darkModeToggle.innerHTML = '<i class="fas fa-moon"></i>';
                } else {
                    document.body.classList.add('dark-mode');
                    localStorage.setItem('theme', 'dark');
                    darkModeToggle.innerHTML = '<i class="fas fa-sun"></i>';
                }
            });
            
            // Video loading event
            const collectionVideo = document.querySelector('.collection-video');
            if (collectionVideo) {
                collectionVideo.addEventListener('loadeddata', function() {
                    this.classList.add('loaded');
                    this.closest('.collection-video-container').classList.add('video-loaded');
                });
            }
            
            // Parallax effect for hero section
            const heroSection = document.querySelector('.hero-section');
            window.addEventListener('scroll', () => {
                if (heroSection) {
                    const scrollPosition = window.scrollY;
                    if (scrollPosition < heroSection.offsetHeight) {
                        const parallaxOffset = scrollPosition * 0.4;
                        const heroSlides = document.querySelectorAll('.hero-slide');
                        heroSlides.forEach(slide => {
                            slide.style.backgroundPositionY = `-${parallaxOffset}px`;
                        });
                    }
                }
            });

            // Video fallback for browsers that don't support video
            const videos = document.querySelectorAll('video');
            videos.forEach(video => {
                video.addEventListener('error', function() {
                    const parent = this.parentElement;
                    const img = document.createElement('img');
                    img.src = '/Uploads/Screenshot 2025-04-07 201605.png';
                    img.alt = 'Formal Collection';
                    img.className = 'collection-video';
                    parent.replaceChild(img, this);
                });
            });

            // Add shimmer effect to review cards
            document.querySelectorAll('.review-card').forEach(card => {
                // Micro-interactions on hover
                card.addEventListener('mouseenter', () => {
                    // Subtle scale effect
                    card.style.transform = 'translateY(-10px)';
                    
                    // Highlight the rating stars
                    const stars = card.querySelectorAll('.review-rating i');
                    stars.forEach((star, index) => {
                        setTimeout(() => {
                            star.style.transform = 'scale(1.2)';
                            setTimeout(() => {
                                star.style.transform = 'scale(1)';
                            }, 300);
                        }, index * 100);
                    });
                });

                card.addEventListener('mouseleave', () => {
                    card.style.transform = 'translateY(0)';
                });
            });

            // "Read more" functionality for long reviews
            document.querySelectorAll('.review-text').forEach(text => {
                const content = text.textContent;
                if (content.length > 150) {
                    const truncated = content.substring(0, 150) + '...';
                    const fullText = content;
                    
                    text.textContent = truncated;
                    text.dataset.fullText = fullText;
                    
                    const readMoreBtn = document.createElement('button');
                    readMoreBtn.className = 'read-more-btn';
                    readMoreBtn.textContent = 'Read more';
                    
                    readMoreBtn.addEventListener('click', function(e) {
                        e.preventDefault();
                        
                        if (text.classList.contains('expanded')) {
                            text.textContent = truncated;
                            text.classList.remove('expanded');
                            readMoreBtn.textContent = 'Read more';
                        } else {
                            text.textContent = fullText;
                            text.classList.add('expanded');
                            readMoreBtn.textContent = 'Read less';
                        }
                        
                        text.parentNode.appendChild(readMoreBtn);
                    });
                    
                    text.parentNode.appendChild(readMoreBtn);
                }
            });

            // Initialize trending section specific functionality
            const trendingImages = document.querySelectorAll('.trending-item .product-image');
            trendingImages.forEach((img) => {
                img.addEventListener('error', function() {
                    this.src = '/placeholder.svg?height=300&width=300&query=shirt';
                });
            });
        });
    </script>

    <!-- Trending Section Specific JavaScript -->
    <!-- Add this script immediately after the trending products section -->
<!-- Force Trending Products Visibility -->
<script>
  // Execute immediately without waiting for DOMContentLoaded
  (function() {
    // Force trending section to be visible
    const trendingSection = document.querySelector('.trending-products-section');
    if (trendingSection) {
      trendingSection.style.display = 'block';
      trendingSection.style.visibility = 'visible';
      trendingSection.style.opacity = '1';
      trendingSection.style.zIndex = '5';
      trendingSection.style.position = 'relative';
    }
    
    // Force trending grid to be visible
    const trendingGrid = document.querySelector('.trending-grid');
    if (trendingGrid) {
      trendingGrid.style.display = 'grid';
      trendingGrid.style.visibility = 'visible';
      trendingGrid.style.opacity = '1';
      trendingGrid.style.zIndex = '10';
    }
    
    // Force all trending items to be visible with smaller size
    const trendingItems = document.querySelectorAll('.trending-item');
    trendingItems.forEach(item => {
      item.style.display = 'block';
      item.style.visibility = 'visible';
      item.style.opacity = '1';
      item.style.zIndex = '1000';
      item.style.transform = 'none';
      
      // Force product cards to be visible with smaller size
      const productCard = item.querySelector('.product-card');
      if (productCard) {
        productCard.style.display = 'flex';
        productCard.style.visibility = 'visible';
        productCard.style.opacity = '1';
        productCard.style.zIndex = '15';
        productCard.style.position = 'relative';
        productCard.style.transform = 'none';
        productCard.style.height = 'auto'; // Allow height to be determined by content
      }
      
      const productCardInner = item.querySelector('.product-card-inner');
      if (productCardInner) {
        productCardInner.style.display = 'flex';
        productCardInner.style.visibility = 'visible';
        productCardInner.style.opacity = '1';
        productCardInner.style.zIndex = '20';
        productCardInner.style.position = 'relative';
        productCardInner.style.transform = 'none';
        productCardInner.style.height = 'auto'; // Allow height to be determined by content
      }
      
      // Force product images to be visible with square aspect ratio
      const productImageWrapper = item.querySelector('.product-image-wrapper');
      if (productImageWrapper) {
        productImageWrapper.style.display = 'block';
        productImageWrapper.style.visibility = 'visible';
        productImageWrapper.style.opacity = '1';
        productImageWrapper.style.position = 'relative';
        productImageWrapper.style.paddingTop = '100%'; // Square aspect ratio
      }
      
      const productImage = item.querySelector('.product-image');
      if (productImage) {
        productImage.style.display = 'block';
        productImage.style.visibility = 'visible';
        productImage.style.opacity = '1';
        productImage.style.position = 'absolute';
        productImage.style.top = '0';
        productImage.style.left = '0';
        productImage.style.width = '100%';
        productImage.style.height = '100%';
        productImage.style.objectFit = 'cover';
        
        // Force reload image
        const currentSrc = productImage.getAttribute('src');
        if (currentSrc) {
          setTimeout(() => {
            productImage.setAttribute('src', currentSrc);
          }, 100);
        }
      }
      
      // Force product info to be visible with smaller padding
      const productInfo = item.querySelector('.product-info');
      if (productInfo) {
        productInfo.style.display = 'flex';
        productInfo.style.visibility = 'visible';
        productInfo.style.opacity = '1';
        productInfo.style.zIndex = '25';
        productInfo.style.padding = '1rem'; // Smaller padding
      }
      
      // Make product name smaller
      const productName = item.querySelector('.product-name');
      if (productName) {
        productName.style.fontSize = '0.95rem';
        productName.style.marginBottom = '0.5rem';
      }
      
      // Make view product button smaller
      const viewButton = item.querySelector('.btn-view-product');
      if (viewButton) {
        viewButton.style.padding = '0.5rem 1rem';
        viewButton.style.fontSize = '0.9rem';
      }
      
      // Make wishlist icon smaller
      const wishlistIcon = item.querySelector('.wishlist-icon');
      if (wishlistIcon) {
        wishlistIcon.style.width = '32px';
        wishlistIcon.style.height = '32px';
        wishlistIcon.style.top = '0.75rem';
        wishlistIcon.style.right = '0.75rem';
      }
    });
    
    // Add a fallback for empty trending section with smaller cards
    setTimeout(() => {
      const trendingGrid = document.querySelector('.trending-grid');
      if (trendingGrid && trendingGrid.children.length === 0) {
        // Create fallback products if none exist
        const fallbackProducts = [
          { 
            name: "Signature Twill Paisley Effect Shirt", 
            price: 3612.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=pink%20dress%20shirt" 
          },
          { 
            name: "Solid Merino Wool Shirt", 
            price: 18275.00, 
            rating: 5, 
            image: "/placeholder.svg?height=300&width=300&query=brown%20dress%20shirt" 
          },
          { 
            name: "Paisley Pulse Twill Shirt", 
            price: 3807.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=paisley%20pattern%20shirt" 
          },
          { 
            name: "Solid Signature Twill Shirt", 
            price: 5727.00, 
            rating: 0, 
            image: "/placeholder.svg?height=300&width=300&query=cream%20dress%20shirt" 
          }
        ];
        
        fallbackProducts.forEach(product => {
          const trendingItem = document.createElement('div');
          trendingItem.className = 'trending-item';
          trendingItem.style.display = 'block';
          trendingItem.style.visibility = 'visible';
          trendingItem.style.opacity = '1';
          trendingItem.style.zIndex = '1000';
          
          trendingItem.innerHTML = `
            <div class="product-card" style="display:flex;visibility:visible;opacity:1;z-index:15;position:relative;height:auto;">
              <div class="product-card-inner" style="display:flex;visibility:visible;opacity:1;z-index:20;position:relative;height:auto;">
                <div class="wishlist-icon" style="width:32px;height:32px;top:0.75rem;right:0.75rem;">
                  <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" class="wishlist-heart" style="width:20px;height:20px;">
                    <path class="heart" d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/>
                  </svg>
                </div>
                <a href="/allproduct" class="product-image-link1">
                  <div class="product-image-wrapper" style="display:block;visibility:visible;opacity:1;position:relative;padding-top:100%;overflow:hidden;">
                    <img 
                      src="${product.image}" 
                      alt="${product.name}" 
                      class="product-image"
                      style="position:absolute;top:0;left:0;width:100%;height:100%;object-fit:cover;display:block;visibility:visible;opacity:1;"
                      onerror="this.onerror=null; this.src='/placeholder.svg?height=300&width=300&query=fashion+product';"
                    >
                  </div>
                </a>
                <div class="product-info" style="display:flex;visibility:visible;opacity:1;z-index:25;padding:1rem;">
                  <div class="product-badge">
                    <span>Trending</span>
                  </div>
                  <h3 class="product-name" style="font-size:0.95rem;margin-bottom:0.5rem;">${product.name}</h3>
                  <div class="product-price-rating" style="margin-bottom:0.75rem;">
                    <p class="product-price">₹${product.price.toFixed(2)}</p>
                    <div class="star-rating" style="font-size:0.8rem;gap:0.15rem;">
                      ${product.rating === 5 ? `
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                        <i class="fas fa-star"></i>
                      ` : `
                        <i class="far fa-star"></i>
                        <i class="far fa-star"></i>
                        <i class="far fa-star"></i>
                        <i class="far fa-star"></i>
                        <i class="far fa-star"></i>
                      `}
                    </div>
                  </div>
                  <a href="/allproduct" class="btn-view-product" style="padding:0.5rem 1rem;font-size:0.9rem;display:flex;align-items:center;justify-content:center;gap:0.5rem;">
                    View Product <i class="fas fa-arrow-right"></i>
                  </a>
                </div>
              </div>
            </div>
          `;
          
          trendingGrid.appendChild(trendingItem);
        });
      }
    }, 500);
  })();
</script>

</body>

</html>
